SE REQUIERE INSTALAR ESTO
1. npm init -y
2. npm install typescript -D
3. npm install ts-node -D
4. npm i nodemon
5. npx tsc --init # esto es para pasar las dependencias de typescript a javascript y crear nuestro tsconfig

6. Despues vienen todos los install y ver si tiene @types los siguientes librerias

npm install ts-node-dev -D --esto es para poder escuchar y compilar los cambios de typescript a javascript
npm install --save-dev @types/node @types/validator
express
cors
dotenv
multer
mongoose
bcryptjs- hashing 
jsonwebtoken

npm WARN deprecated @types/dotenv@8.2.0: This is a stub types definition. dotenv provides its own type definitions, so you do not need this
# TABLAS QUE TENEMOS QUE CREAR

Branches- Ready
Categories- Ready
Companies
Customers
Menus
Migrations
PasswordResets
Payments
Permissions
Products - Ready
Roles - Ready
Sells
sell_details
Stocks
Users - In process
Vendors - Ready
